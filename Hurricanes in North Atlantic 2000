SELECT
    sid AS Storm_ID,
    name AS Storm_Name,
    basin AS Ocean,
    season AS Year
FROM
    `bigquery-public-data.noaa_hurricanes.hurricanes`
WHERE
    Year = 2000
    
## This shows filtering data in SQL for a specifc year

SELECT
    sid AS Storm_ID,
    name AS Storm_Name,
    basin AS Ocean,
    season As Year
FROM
    `bigquery-public-data.noaa_hurricanes.hurricanes`
WHERE
    basin = 'NA'
ORDER BY
    season DESC
    
 ## Shows ORDERBY and Conditional searching on SQL
 

